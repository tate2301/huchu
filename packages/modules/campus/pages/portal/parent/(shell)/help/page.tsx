import { ParentHelpScreen } from "../../../../../components/portal/parent/parent-help-screen";

/** The shell's layout owns the guard and the household — see `(shell)/layout.tsx`. */
export default function Page() {
  return <ParentHelpScreen />;
}

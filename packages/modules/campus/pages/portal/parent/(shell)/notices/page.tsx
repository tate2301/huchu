import { ParentNoticesScreen } from "../../../../../components/portal/parent/parent-notices-screen";

/** The shell's layout owns the guard and the household — see `(shell)/layout.tsx`. */
export default function Page() {
  return <ParentNoticesScreen />;
}

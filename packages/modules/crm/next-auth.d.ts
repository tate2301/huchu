// The session's shape is the kernel's: see packages/platform/auth-core/session-shape.ts.
// A package whose screens read `session.user` references it once, here, as the host does.
import "@corelithzw/platform/auth-core/session-shape";

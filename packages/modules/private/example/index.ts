// A private module's entry carries its manifest, as a product module's does;
// its host imports the manifest by path (`@corelithzw/private-example/manifest`).
export { manifest } from "./manifest";

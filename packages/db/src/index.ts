// The generated types, the `Prisma` namespace and every enum, for any package —
// safe in client bundles, because a bundler resolves `@prisma/client` to its
// browser build here. The database connection lives in `@corelithzw/db/client`
// and must only be imported by server code.
export * from "@prisma/client";

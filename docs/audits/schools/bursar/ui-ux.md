# Bursar dashboard: UI/UX audit

Surface: `app/schools/finance/**` and their content components `components/schools/fees/{fees-grade-picker,class-fees-content,schools-fees-content,fee-dialogs,bulk-generate-invoices-dialog,copy-structure-dialog,fee-status}.tsx`, `components/schools/reports/reports-arrears-content.tsx`, plus the fee tabs of `documents/school-documents-content.tsx`. Audited from source and from `docs/screenshots/schools/fees/*`, `finance-desktop/*`, `finance-phone/*`, `visual-pass-*/01-class-fees.png`. Workflow companion: `workflows.md` in this folder.

Rules applied: `docs/ux/platform-ux-playbook.md`, `docs/design-system/{05-rules,08-cookbook-patterns,12-tables}.md`, `09-campus-canvas-law.md`, `10-campus-screen-contract.md`, `11-campus-states-and-motion.md`, `.impeccable.md`, `SPEC.md`. The rule digest is in `../reference/docs-baseline.md` §9 and `../reference/backoffice-ui-ux-evidence.md` §0.

## 1. Verdict in one paragraph

The bursar's screens have the best confirmation and reason-capture copy in the module ("$ 510.00 is unwound: every invoice this receipt settled goes back to owing. This cannot be undone.") and the receipt dialog's allocation logic is right. They fail on the surface in ways a bursar meets every hour. Row verbs on every fee screen are clipped off the right edge at desktop, tablet and phone widths because `RecordActions` is rendered inline, so "Take payment" reads "Tak". The landing page has no primary action; "Record receipt" is two screens away. The ledger on a phone spends the entire first screen on chrome and shows no invoice. Three surfaces compute ageing in three code paths with different bucket labels (they agreed on the seeded data, and disagreed in an earlier screenshot set). The bill's status vocabulary differs between the ledger and the students list. And the arrears and reports pages ship the designer's rationale as product copy.

Three of those five are answered. Every fee row now keeps the one verb its state invites inline — Issue on a draft, Take payment on a bill still owing, Pay on a requested refund, Approve on a draft waiver, Activate on a draft sheet — and folds the rest into a menu, so nothing is clipped and nothing red sits at rest under a thumb. The phone ledger drops the heading and the caption, carries two chips about the view it is in, folds its filters behind one control on a phone only, uses the table's own pager and gives invoice rows a two-line renderer, so a bill is on the first screen. The ageing surfaces were worse than the audit said and are now one service; the rationale cards are gone from both pages. The landing page is the one the audit found: it still names itself twice, still has no primary, and "Record receipt" is still two screens away. The receipt dialog is untouched, and the documents screen still has no receipt or statement tab.

## Runtime check (14 September 2026)

Verified on the seeded St Marys tenant (see `../reference/runtime-verification.md`). Confirmed at runtime: 28 of 49 buttons on the class fees page sit past the right edge of a 1440px viewport ("Take payment", "Write off" clipped); the first invoice on the phone ledger sits at 973px, below an 844px screen; the rationale cards on arrears and reports render; the "Outstanding 29,910" chip has no currency; the bursar's sidebar shows Result sheets, Homework and Scheme of work. Corrected: the ageing surfaces agree on this data.

Revised on 15 September 2026 against the eighteen commits that followed the audit (`01de8a3..HEAD`). A finding the work closed keeps its description and gains a line saying it is closed and what closed it; a finding the work did not reach stands as it was written; a finding the implementation proved wrong says so and says what is actually true. The runtime paragraph above is left as it was recorded, including its last sentence, which is wrong: the surfaces agreed on the seeded data, which is not the same as agreeing, and they did not agree in general. The correction is at §4 and in the workflow audit's B10.

## 2. Screen-by-screen

| Route | Component | Contract violations | Friction | Role awareness | Phone | Severity |
|---|---|---|---|---|---|---|
| `/schools/fees` | redirect to `/schools/finance` | Nav says Fees, URL says finance | | | | P2 |
| `/schools/finance` | `fees-grade-picker.tsx` | In-page `PageHeading` "Fees and finance" plus app-bar "Finance"; seven band chips including "Classes 0" beside "Year groups 6"; three band actions and no primary; year-group table in a card, not full bleed; the collected progress bar painted danger red; "Find a year group" search alone on its row. All unchanged; the component was not touched | Rows are not visibly links; "Longest overdue" rows have no verb; "Send reminders" goes to all overdue with no preview of who. All unchanged | No `useSchoolAccess`; reminder button not gated on the route's real permission. The mismatch is resolved from the other end (`e5b76a5`): the route now takes `notify-families`, which the bursar holds, so the button sends rather than 403s. The screen still asks `fees:create` and still does not use `useSchoolAccess` | Six numeric columns will scroll sideways. Unchanged. The ageing figures beneath them are now correct: the SQL behind this panel called a bill due today overdue and was shifted to the shared boundaries (`f6556ee`), although the panel still draws its own bars rather than the shared strip | P1 |
| `/schools/finance/class/[classId]` | `class-fees-content.tsx` | `PageHeading` "Form 1 fees" plus bar "Year group"; paragraph repeating the band chip (`:206`); inline `RecordActions` "Take payment · Write off · Edit" stacked and clipped (`:326-360`); "Write off" as a solid red button on every unpaid row. Fixed in `c65e58a`: the row keeps one verb inline — Issue on a draft, Take payment on a bill still owing — and folds Write off and Edit into the menu, so the red verb is no longer the widest thing on the row and nothing is clipped. The heading under the bar and the paragraph repeating the chip are unchanged | Record a receipt from here is two clicks, the best path in the module, if the button were visible; no family-level payment; no running total of today. The button is visible now; the other two stand | Verbs gated by `RecordActions` | Rows already card-shaped; verbs still clip. Fixed with the rest | P0 |
| `/schools/finance/ledger?view=invoices` | `schools-fees-content.tsx` (2,197 lines) | `PageHeading` "Fee ledger" with a caption repeating the chips (`:1352-1357`); each panel adds an `h2` (fourth name); six filters with two native date inputs (`:1461, :1472`) and "Owing at least" without currency; "Showing the first 100 of 120" banner instead of the pager; a second search row; inline row verbs on invoices (`:723`), refunds (`:994`), waivers (`:1137`), structures (`:1270`) while receipts (`:825`) and credits (`:925`) correctly use `layout="menu"`. Fixed in `c65e58a`: the heading and the caption are gone, the chips are two per view and about that view, the banner is the table's own pager, the native inputs are the design-system `DatePicker`, and every view uses `layout="menu"` with at most one inline primary | Two "Record receipt" primaries on the empty receipts view; no bulk issue for drafts; no saved views; no keyboard. All four stand. Selection now offers one bulk verb, Remind, because that is the one the API can perform in a single call; bulk issue, print, activate and approve have no endpoint and were deliberately not faked by fanning out browser requests. An invoice also gained the "Take payment" verb it never had | `CreateButton` per view gated correctly | First invoice below the fold at 390×844; no `mobileListRenderer` so a nine-column table scrolls sideways. Fixed: invoice rows have a two-line phone renderer with the balance right-aligned, and the six filters fold behind one control with a count on a phone only — a desktop has room for them and hiding them there would cost a press for nothing. The phone row opens the payment dialog, but only when there is a balance and the reader may take payment, so the chevron never promises what the permission model refuses | P0 |
| `…?view=receipts` | same | Nine columns. Unchanged | Void confirm copy exemplary | gated | none | P1 |
| `…?view=credits`, `…?view=refunds` | same | Refund "Pay / Cancel" inline. Fixed in `c65e58a`: Pay stays inline on a requested refund, Cancel is in the menu | Raising a refund from a credit lands on Credits while the refund lives on Refunds, with no link. Unchanged | gated | none | P2 |
| `…?view=waivers` | same | Up to six inline buttons per row. Fixed in `c65e58a`: Approve stays inline on a draft, the rest are in the menu | Approve-now toggle in the form is good. It is now inert and misleading: the create route dropped `status` from its schema, so the checkbox labelled "Approve it now, rather than leaving it a draft" is stripped from the request, the waiver is created as a draft and nothing on screen says the box did nothing (`fee-dialogs.tsx:377, 496-499`; workflow audit B17). Approval itself is reachable — it is the inline verb on a draft waiver row | gated | none | P1 |
| `…?view=structures` | same | Inline verbs; "New fee sheet" vs tab "Fee structures" vs nav "Ledger and structures". The verbs are fixed (`c65e58a`): Activate inline on a draft sheet, the rest in the menu. The three names for one thing are unchanged | No preview before bulk generate. Unchanged | gated | none | P2 |
| `/schools/finance/arrears` | `reports-arrears-content.tsx` | `PageChrome` with "Remind the N" primary (good); `toFixed` and `toLocaleString` for money; rationale cards "The missing verb" and "Why these buckets" rendered as UI (`:797-812`). Fixed in `f6556ee`: both cards are gone, money goes through `formatSchoolMoney`, the Title Case headings are down to sentence case and the bands are the shared service's. One thing the audit did not record and should have: the app-bar title is `PageChrome title="School Reports"` (`:442`) — Title Case, and the name of a different screen, on a route that lives under Finance | No per-family "Take payment" from the row; family phone not on the row. Both unchanged | `useSchoolAccess` used; button correctly disabled for BURSAR, which means the bursar cannot chase from here at all. It is no longer correctly disabled: the route behind it takes `notify-families`, which the bursar holds, and the screen still asks `schools.reports` create, which she does not — so both the bar primary and the row's Remind stay dark, and the tooltip still says "Writing to families is the head's to do." See the workflow audit's B13 | none | P0 (copy leak) |
| `/schools/reports` (arrears tab) | `schools-reports-enhanced-content.tsx` | Chip "Outstanding 29,910" with no currency (`:715`, confirmed at runtime); the chart and chip agreed with the finance page on the seeded data but are computed separately; "The missing verb" and "What the screen cannot do" cards (`:1090`) confirmed at runtime. Fixed in `f6556ee`: the chip goes through `formatSchoolMoney`, the cards are gone, and the bands are read from `lib/schools/ageing.ts` rather than computed here. The sentence about the chart is the audit's mistake and is corrected at §4: the surfaces agreed on the seeded data and did not agree in general | Export in the band is good. Its last column was labelled "120+ days" for a column that has always held everything past ninety; that is fixed too | | none | P0 (copy leak) |
| `/schools/documents` (fee invoice tab) | `school-documents-content.tsx` | Title Case throughout; raw enum status printed. The Title Case is swept (`272edf4`); the invoice preview still prints `invoice.status` raw (`:411`) | Receipt and statement have no tab; no batch print. Both unchanged — the screen still carries four views, and the report card among them turns out not to print at all unless a publish window is open, which no seeded school has (head audit B14) | | none | P1 |

## 3. Screenshot findings

The captures are from 14 September and are left as they were. Where the screen has since changed, the change is named at the end of the entry.

- **Finance overview (desktop).** Good: per-form billed, collected and outstanding with a bar and percentage; ageing list; longest-overdue list with "90d" in mono red; currency caption. Wrong: page named twice; seven chips; no primary; collected bars in red; rows not clickable; the search alone on a row. All still true; only the figures in the ageing list moved, to the shared boundaries.
- **Invoices.** Good: mono invoice numbers, `$ 510.00`, `20 September 2026`, status badges. Wrong: header, caption and bar all name the page; six filters with two `mm/dd/yyyy` inputs; "first 100 of 120" banner; a second search row; "Edit / Write off (red) / Print" inline per row with Print clipped; rows around 90px. All but two are fixed: the header and caption are gone, the dates are the design-system picker, the banner is the pager, and the row carries Take payment inline with the rest in a menu. The second search row and the row height stand.
- **Arrears.** Good: "Remind the 75" in the bar; CSV and PDF export. Wrong: "Outstanding 29,910"; Title Case tabs; filters over two rows; the chart contradicting the chip; rationale cards; a second search below the chart. The chip, the Title Case, the contradiction and the cards are fixed; the filters over two rows and the second search stand, as does a Title Case app-bar title naming the wrong screen. "Remind the 75" is now dark for the bursar for a permission the route no longer asks (workflow audit B13).
- **Receipts.** Good: the empty-state sentence. Wrong: two "Record receipt" primaries while the view is empty (one once a receipt exists); table header, empty state and pagination all rendered; four filters including two native dates. The native dates are now the design-system picker and the filters fold behind one control on a phone; the two primaries on an empty view stand.
- **Finance phone (390×844).** No record visible on the first screen: five chips over three rows, a tab strip clipped mid-word, six stacked filters, the banner, the search at the bottom edge. Fixed for the ledger (`c65e58a`): two chips, the filters behind one control with a count, the pager in place of the banner, and a two-line invoice row.
- **Class fees (desktop, tablet, phone).** Good: rows are the right shape (avatar, "Surname, First", mono invoice number, term, billed, outstanding, status). Wrong: three stacked verbs clipped at every width; red "Write off" per row; caption repeating the chip; bar title "Year group". The verbs and the red button are fixed; the caption and the bar title stand.

## 4. Rule violations that recur

| Rule | Violation | Where |
|---|---|---|
| A page is named once | In-page heading plus bar title on overview, class, ledger. Fixed on the ledger (`c65e58a`); the overview and the class page still draw a `PageHeading` from their route files under a bar that carries the name | three screens |
| One primary in the bar | None on overview; two on the receipts view. Both stand | two screens |
| Row verbs in a menu; one inline primary | Inline stacks clipped. Fixed on all five (`c65e58a`), and on the class page: one inline primary chosen by the row's state, the rest in a menu | five views |
| One control row, one search | Two search rows on the ledger and arrears. Both stand | two screens |
| Money carries currency; dates as `3 June 2026` | Chip without currency; `toFixed`; native dates. The chip and the arrears money go through `formatSchoolMoney` (`f6556ee`) and the ledger's filter dates are the design-system picker (`c65e58a`); native `type="date"` inputs remain in the receipt, waiver and bulk-generate dialogs | three screens |
| Never the same fact twice | Caption repeating chips; "Classes 0". Fixed on the ledger; the class page's caption and the overview's "Classes 0" stand | three screens |
| Colour marks what to stop on | Collected bar in red; "Write off" as a red button at rest. The red Write off is a menu item now, its tone kept for the confirmation; the collected bar is still painted danger red | two screens |
| Strip explanatory copy | Rationale cards. Fixed (`f6556ee`); both cards on each screen are gone, and the reasoning about the buckets went into `lib/schools/ageing.ts` | arrears, reports |
| One status vocabulary | Ledger: Draft, Issued, Part paid, Paid, Written off, Voided; students list: Paid, Partial, Overdue. Half fixed: "Overdue" is a filter on the ledger, not a status; the students list still badges a pupil Paid, Partial, Overdue (`students-list-content.tsx:138`) | two screens |
| The first phone screen belongs to the records | No invoice above the fold. Fixed (`c65e58a`) | ledger |
| Ratios with a zero denominator render as "—" | "0%" on the dashboard fee panel. Fixed on the dashboard and the reports page (`f6556ee`): a rate with nothing behind it reads "—" and paints neutral rather than danger red | overview |
| Ageing is one definition | Recorded as three computations that agreed on the seeded data. This was wrong, and the audit should not have softened it. There were four — the finance overview's SQL, the reports page, the dashboard and the server's report builder — and they did not agree: a bill due today was overdue on the overview and Current in the arrears report, because a due date is stored at midnight and only the report counted whole days. The export's last column was labelled "120+ days" for a 90+ column. Fixed (`f6556ee`): `lib/schools/ageing.ts` holds the boundaries and the labels, one `AgeingStrip` draws them, the report builder asks the same service, and the SQL was shifted to match | four surfaces |

## 5. Suggested edits

Numbers 1, 2, 3 and 5 are done, and 4, 8, 9 and 12 in part, in the commits named against the entries in §2; the rest stand. Where what was built differed from what was proposed, the difference is recorded under its item.

1. **Row verbs (P0).** `layout="menu"` on `class-fees-content.tsx:326`, `schools-fees-content.tsx:723, 994, 1137, 1270`, with one inline primary per row where the row has an obvious verb: "Take payment" on an unpaid invoice, "Approve" on a submitted waiver, "Pay" on a requested refund. The receipts view already does this. Done as proposed, with one more inline verb than the list names — Issue on a draft bill and Activate on a draft fee sheet — and an invoice gained the "Take payment" verb it never had at all.
2. **One ageing (P1).** One endpoint, one bucket definition, one `AgeingStrip` component consumed by the overview, the reports page and the dashboard, so the three cannot drift again. Done, and by four consumers rather than three; the server's report builder was the one the finding missed. The definition is a service rather than an endpoint, `lib/schools/ageing.ts`, and the finance overview still draws its own bars from corrected figures rather than the shared strip.
3. **Phone ledger (P0).** Drop `PageHeading`, drop the caption, keep two chips per view, put the six filters behind one "Filter" sheet with a count badge, use the DataTable pager instead of the banner, add a `mobileListRenderer` (two lines, balance on the right, chevron). That lands the first invoice near y=300. Done as proposed, with the filter sheet applied on a phone only: a desktop has room for the row, and hiding it there would cost a press for nothing.
4. **Currency and dates (P1).** `schools-reports-enhanced-content.tsx:715` through `formatSchoolMoney`; every `toLocaleString` and `toFixed` in the fee and report components through `lib/schools/format.ts`; native date inputs to the design-system picker (sheet on phone). Done for the chip, for the arrears and reports money, and for the ledger's own filter dates. The receipt, waiver and bulk-generate dialogs still use `type="date"`, and the remaining `toFixed` calls seed form fields and format percentages rather than print money.
5. **Remove rationale cards (P0).** `reports-arrears-content.tsx:797-812`, `schools-reports-enhanced-content.tsx:1090-1100` to comments or docs. Done. The reasoning about the bucket boundaries went into `lib/schools/ageing.ts`, where it belongs.
6. **Landing primary (P1).** `PageChrome` with "Record receipt" opening `ReceiptFormDialog` with an `InvoicePicker`; year-group rows as links; a `···` on longest-overdue rows with "Take payment" and "Remind". Not done; the component was not touched.
7. **Receipt dialog (P1).** Show "Outstanding on SFI-00120: $ 510.00" under the picker; live "Balance after / Credit created" as the amount is typed; method as a labelled select defaulting to Cash; date via the picker; a "Print receipt" checkbox that fires the receipt document after save. Not done; `fee-dialogs.tsx` is untouched. That is also where the waiver form's now-inert "Approve it now" checkbox lives (B17), and the file should be revisited for both at once.
8. **Status vocabulary (P1).** `InvoiceStatusBadge` everywhere; "Overdue" as a filter chip (due date past and balance above zero), not a status. Half done: "Overdue" is a filter on the ledger, defined exactly as proposed. The students list still badges a pupil's fee standing Paid, Partial, Overdue.
9. **Bulk actions (P1).** Selection bar on invoices (issue drafts, print, remind), on structures (activate), on waivers (approve). One of the five shipped: Remind, on invoices. Bulk issue, print, activate and approve have no endpoint behind them, and fanning out a request per row from the browser would have looked like a bulk action while behaving like none — so they were left out rather than faked.
10. **Bulk generate preview (P2).** "This will raise 20 invoices totalling $ 9,800" before confirm. Not done.
11. **Refund thread (P2).** After raising a refund, switch to `?view=refunds` and highlight the new row, or toast "Open refund RF-0007". Not done.
12. **Documents (P1).** Add receipt and statement tabs; batch statement by class; sentence case. Sentence case is done (`272edf4`). The tabs and the batch statement are not, and the invoice preview still prints a raw enum status.

## 6. Proposed restructuring

- **`/schools/finance` becomes the bursar's dashboard** (see §7), with the year-group table below the hero. Not done; the screen is the year-group picker it was. It is at least where the bursar now lands, because the rail is generated from one source and filtered by persona (`72f9227`).
- **The ledger's six views stay as one route** with `?view=`, but the filters collapse to the two that matter per view plus a sheet for the rest; saved views ("Owing over $500 in Form 3", "Unfiscalised this week") as chips in the strip. Half done: the chips are two per view and about that view, and the filters fold behind one control with a count, on a phone. Saved views are not built.
- **A family view** reached from any invoice or receipt: household, all children, balance, statement, receipts, reminders sent, calls logged. Not done.
- **Reminders and calls live on the arrears row**: "Remind" (send), "Log a call" (outcome and note, no send), "Take payment". The bursar must be able to use all three. Not done, and the last sentence is now the sharper point: the bursar holds the grant the route asks for and the arrears screen still asks for a different one, so she can use none of the three from here (workflow audit B13).
- **Rename the nav band "Fees"** and keep it as the bursar's first band with `preferredHomeHref = /schools/finance`. Done (`72f9227`): the rail is derived from `lib/navigation.ts` and filtered by persona, each persona lands somewhere it can work, and band icons were added, without which Fees opened with a warning triangle.

## 7. Proposed new UI

None of the screens below has been built. One line in this section has landed, at the end of the interaction-patterns paragraph: route-level `loading.tsx`, `error.tsx` and `not-found.tsx` now cover `app/schools`, finance included, with skeletons that mirror the real layout rather than standing in for it, and the class routes that call `notFound()` send a person back to the list they opened the class from (`0089c5b`).

**Bursar dashboard (`/schools/finance`)**
- Bar primary: Record receipt. Band chips: Outstanding, Overdue with family count, Collected this term %, Credit on account.
- Hero: Collected vs billed this term with a progress bar and "same day last term" delta.
- KPIs (two-up on phone): Today's takings with receipt count (link to receipts filtered to today); Draft invoices not issued (link to issue all); Receipts not fiscalised (link to the ZIMRA queue); Refunds awaiting payment.
- Year-group collection table with row links and a `···`.
- Panels: Ageing (the one component); Longest overdue with verbs; Recent receipts, printable; Payment-method mix this week for cash-up.
- Empty state on a new term: "No invoices raised for Term 3 yet" with "Bulk generate" as the verb.

**Counter mode.** A focused receipt screen for the office window: student or family search, outstanding invoices with amounts, amount and method, change due for cash, print on save, next family. One screen, keyboard-first, usable on a tablet.

**Cash-up screen.** Per cashier per day: opening float, receipts by method, counted cash, variance, deposit slip, close.

**Reminder composer.** Preview of who will receive it (name, child, balance, channel), template with merge fields, cost estimate for SMS, send, then delivery status on the arrears row.

**Interaction patterns to add:** command palette verbs ("Record receipt", "Go to arrears"), URL-synced filters and saved views, undo toast for discard, peek on the student in an invoice row, a date stepper for receipts by day, and route-level `loading.tsx` and `error.tsx` under `app/schools/finance`. The last of these is done, at the segment root rather than under finance: one skeleton, one client error boundary wired to its own reset quoting the digest, and one not-found, with no second boundary below, because a failure is scoped to the panel that failed and the whole screen is kept for the unrecoverable page.

## 8. Accessibility

- Row verbs hidden by clipping are also unreachable to keyboard users who cannot see focus land off-screen; the menu fixes both. Done (`c65e58a`).
- The red "Write off" button at rest has the same tone as an error; keep destructive tone for the confirm, neutral at rest. Answered by the menu rather than by a tone change: Write off is a menu item now and the destructive tone survives where it is acted on, in the confirmation.
- Money in a monospace face with slashed zeros is hard to read at 11px; keep tabular figures in the sans face for amounts and mono for references. Not done on these screens. The parent portal was rebuilt on that rule (`ed82dc1`), so the two now disagree.
- Native date inputs announce in the browser's locale; the picker fixes the format and the announcement. Done for the ledger's filter row; the receipt, waiver and bulk-generate dialogs still use a native input.

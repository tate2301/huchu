import { ParentFeesScreen } from "@corelithzw/module-campus/components/portal/parent/parent-fees-screen";

/** The shell's layout owns the guard and the household — see `(shell)/layout.tsx`. */
export default function Page() {
  return <ParentFeesScreen />;
}

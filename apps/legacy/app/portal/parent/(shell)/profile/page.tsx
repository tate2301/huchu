import { ParentProfileScreen } from "@corelithzw/module-campus/components/portal/parent/parent-profile-screen";

/** The shell's layout owns the guard and the household — see `(shell)/layout.tsx`. */
export default function Page() {
  return <ParentProfileScreen />;
}

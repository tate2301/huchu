import { ParentMarksScreen } from "@corelithzw/module-campus/components/portal/parent/parent-marks-screen";

/** The shell's layout owns the guard and the household — see `(shell)/layout.tsx`. */
export default function Page() {
  return <ParentMarksScreen />;
}

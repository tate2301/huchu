import { StudentLibraryScreen } from "@/components/schools/portal/student/student-library-screen";

/** The library as a shelf of covers, plus what the pupil has out. */
export default function StudentLibraryPage() {
  return <StudentLibraryScreen />;
}

import { StudentTimetableScreen } from "@corelithzw/module-campus/components/portal/student/student-timetable-screen";

/** The pupil's own week, a day at a time. */
export default function StudentTimetablePage() {
  return <StudentTimetableScreen />;
}

import { StudentGoalsScreen } from "@/components/schools/portal/student/student-goals-screen";

/** The pupil's goals. Auth and chrome are the layout's job. */
export default function StudentGoalsPage() {
  return <StudentGoalsScreen />;
}
